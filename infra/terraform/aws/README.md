# AWS Reference Architecture

This directory captures the AWS-oriented shape of the lab using the same naming inputs as the executable LocalStack stack.

It is intentionally reference-grade. The contract is validated through Terraform syntax and module composition, while the repository keeps execution local-first through LocalStack instead of claiming a live AWS deployment.
