# GCP Reference Architecture

This directory keeps the provider, variable, and naming model for a GCP-oriented adaptation of the lab.

It is a reference implementation for review and extension rather than an applied environment. The executable infrastructure path in this repository remains the LocalStack stack under `infra/terraform/localstack`.
