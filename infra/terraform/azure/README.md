# Azure Reference Architecture

This directory documents how the local-first workflows in this repository map onto an Azure-native platform shape.

The files remain syntax-valid Terraform so reviewers can inspect provider wiring, variables, and naming decisions without implying that the repository provisions paid Azure resources.
