# Terraform Modules

`platform_context` provides naming and label conventions shared across the repository.

`lambda_function` wraps the Lambda packaging contract used by the executable LocalStack stack. The same inputs are reused by the reference cloud directories to keep naming, tags, and runtime metadata consistent.
