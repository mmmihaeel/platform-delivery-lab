apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-config
  namespace: platform-delivery-lab
data:
  PLATFORM_ENV: local
  PLATFORM_VERSION: "1.0.0"
  HEALTH_PATH: /healthz
  STATUS_PATH: /status
